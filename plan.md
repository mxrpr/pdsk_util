## Plan
- [x] Part 1: Remove dead/redundant code — In `pdsk_util.py`, drop the unused `as e` binding in the `except OSError` handler (lines 69-70) and the redundant `if specific_mount and` re-check inside the already-guarded block (line 55). Also remove `setuptools` from `requirements.txt` (it's not a runtime dependency).
- [x] Part 2: Human-readable size formatting — In `pdsk_util.py`, replace the `usage.total // (1024**3)` integer-GB math (which shows sub-GB volumes as `0G` and never scales to TB) with a helper that auto-selects B/K/M/G/T units, so small partitions and large arrays both render correctly.
- [x] Part 3: Fix specific-mount filtering — Reorder `print_disk_usage` so that when a `specific_mount` is explicitly requested, the fstype/mountpoint noise filters (lines 59-62) are skipped; currently asking for a `vfat`/`/run` mount silently yields "No readable filesystems". Improve the not-found message to distinguish "no match for requested volume" from "no readable filesystems".
- [x] Part 4: Reconsider the noise filter — In `pdsk_util.py`, remove `vfat` from the default skip set (it hides real USB/removable drives) and review the hardcoded Linux `/sys,/proc,...` prefixes so Windows drives aren't unintentionally affected; keep the filter minimal and correct.
- [x] Part 5: Proper argument parsing — Replace the ad-hoc `sys.argv` handling in `main()` with `argparse`, keeping the positional `VOLUME` and `-h/--help`, and add `--version` (sourced from a single `__version__`/setup version). Removes the manual help table and the `argv[1]`-only help check.
- [x] Part 6: Top-level robustness and stable ordering — Wrap the `psutil.disk_partitions()` call in `print_disk_usage` with error handling for the case where it raises or returns nothing, add a top-level `try/except` in `main()` for a clean error exit code, and sort partitions by mountpoint so output ordering is deterministic.
